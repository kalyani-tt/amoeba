Bool : Type
tt : Bool
ff : Bool
if : (A : Type) -> Bool -> A -> A -> A
if-tt : (A : Type) -> (x : A) -> (y : A) ->
        if A tt x y = x
if-ff : (A : Type) -> (x : A) -> (y : A) ->
        if A ff x y = y
